<UserControl x:Class="GamerGuardian.UI.Views.ServicesView"              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"              xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"              d:DesignHeight="600" d:DesignWidth="760"              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"              mc:Ignorable="d">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="4,8,8,0">
                        <StackPanel>
                            <ui:Card Padding="16" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="Stop and disable Windows services that run by default but typically aren't needed for gaming. Each service is monitored against your preference and (optionally) re-applied when Windows or another app re-enables it."
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               FontSize="12" TextWrapping="Wrap" Margin="0,0,0,8"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="One-click preset:" Margin="0,4,12,0"/>
                                        <RadioButton x:Name="ServicesPresetGaming" GroupName="services_preset"
                                                     Content="Gaming optimized" Margin="0,0,12,0"
                                                     Checked="ServicesPresetGaming_Checked"/>
                                        <RadioButton x:Name="ServicesPresetDefault" GroupName="services_preset"
                                                     Content="Default"
                                                     Checked="ServicesPresetDefault_Checked"/>
                                    </StackPanel>
                                    <TextBlock Text="Tip: the Gaming preset toggles the recommended subset; non-recommended services stay where you set them."
                                               Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                               FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"/>
                                </StackPanel>
                            </ui:Card>
                            <ItemsControl x:Name="ServicesList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ui:Card Padding="16" Margin="0,0,0,8">
                                            <StackPanel>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                                    <WrapPanel Margin="0,4,0,0">
                                                        <Border Background="{DynamicResource SubtleFillColorTertiaryBrush}"
                                                                CornerRadius="3" Padding="6,1" Margin="0,0,6,4">
                                                            <TextBlock Text="{Binding ServiceName}"
                                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                                       FontSize="10" FontFamily="Consolas"/>
                                                        </Border>
                                                        <Border Visibility="{Binding RecommendedBadgeVisibility}"
                                                                Background="{DynamicResource SystemFillColorSuccessBackgroundBrush}"
                                                                CornerRadius="3" Padding="6,1" Margin="0,0,6,4">
                                                            <TextBlock Text="recommended"
                                                                       Foreground="{DynamicResource SystemFillColorSuccessBrush}"
                                                                       FontSize="10" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <Border Visibility="{Binding RebootBadgeVisibility}"
                                                                Background="{DynamicResource SystemFillColorCautionBackgroundBrush}"
                                                                CornerRadius="3" Padding="6,1" Margin="0,0,6,4">
                                                            <TextBlock Text="reboot required"
                                                                       Foreground="{DynamicResource SystemFillColorCautionBrush}"
                                                                       FontSize="10" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <Border Visibility="{Binding NotInstalledBadgeVisibility}"
                                                                Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                                                                CornerRadius="3" Padding="6,1" Margin="0,0,6,4">
                                                            <TextBlock Text="not installed"
                                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                                       FontSize="10" FontWeight="SemiBold"/>
                                                        </Border>
                                                    </WrapPanel>
                                                    <TextBlock Text="{Binding Description}"
                                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                               FontSize="12" Margin="0,3,0,0"
                                                               TextWrapping="Wrap"/>
                                                    <WrapPanel Margin="0,6,0,0">
                                                        <TextBlock Text="{Binding CurrentText}"
                                                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                                   FontSize="11" Margin="0,0,12,2"/>
                                                        <TextBlock Text="{Binding DefaultText}"
                                                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                                   FontSize="11" Margin="0,0,12,2"/>
                                                        <TextBlock Text="{Binding RecommendedText}"
                                                                   Visibility="{Binding RecommendedTextVisibility}"
                                                                   Foreground="{DynamicResource SystemFillColorSuccessBrush}"
                                                                   FontSize="11" FontWeight="SemiBold" Margin="0,0,0,2"/>
                                                    </WrapPanel>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Orientation="Vertical"
                                                            IsEnabled="{Binding IsInstalled}">
                                                    <CheckBox Content="Monitor" IsChecked="{Binding Monitor}"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                                                        <TextBlock Text="Want:" Margin="0,4,8,0"/>
                                                        <RadioButton GroupName="{Binding GroupName}" Content="Default" IsChecked="{Binding DesiredDefault}" Margin="0,0,6,0"/>
                                                        <RadioButton GroupName="{Binding GroupName}" Content="Manual" IsChecked="{Binding DesiredManual}" Margin="0,0,6,0"/>
                                                        <RadioButton GroupName="{Binding GroupName}" Content="Disabled" IsChecked="{Binding DesiredDisabled}"/>
                                                    </StackPanel>
                                                    <CheckBox Content="Auto-apply silently" IsChecked="{Binding AutoApply}"/>
                                                </StackPanel>
                                            </Grid>
                                                <Expander Header="Learn more" Margin="0,8,0,0"
                                                          Visibility="{Binding LearnMoreVisibility}">
                                                    <TextBox Text="{Binding LearnMoreContent, Mode=OneWay}"
                                                             Style="{StaticResource CopyableLearnMoreText}"
                                                             Margin="8,8,8,8"/>
                                                </Expander>
                                            </StackPanel>
                                        </ui:Card>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <ui:Card Padding="16" Margin="0,8,0,8">
                                <StackPanel>
                                    <TextBlock Text="Scheduled tasks" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="The Windows &quot;Application Experience&quot; scheduled tasks run CompatTelRunner.exe, whose periodic compatibility/telemetry scans spike CPU and disk -- an unpredictable hitch source during gaming. Disabling them is safe and reversible; Windows Update keeps working. A feature update can re-enable them, so monitor + auto-apply re-disables on drift. These are included in the General tab's Recommended preset."
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                            </ui:Card>
                            <ItemsControl x:Name="ScheduledTasksList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ui:Card Padding="16" Margin="0,0,0,8">
                                            <StackPanel>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                                    <WrapPanel Margin="0,4,0,0">
                                                        <Border Background="{DynamicResource SubtleFillColorTertiaryBrush}"
                                                                CornerRadius="3" Padding="6,1" Margin="0,0,6,4">
                                                            <TextBlock Text="{Binding TaskPath}"
                                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                                       FontSize="10" FontFamily="Consolas" TextWrapping="Wrap"/>
                                                        </Border>
                                                        <Border Visibility="{Binding RecommendedBadgeVisibility}"
                                                                Background="{DynamicResource SystemFillColorSuccessBackgroundBrush}"
                                                                CornerRadius="3" Padding="6,1" Margin="0,0,6,4">
                                                            <TextBlock Text="recommended"
                                                                       Foreground="{DynamicResource SystemFillColorSuccessBrush}"
                                                                       FontSize="10" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <Border Visibility="{Binding NotPresentBadgeVisibility}"
                                                                Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                                                                CornerRadius="3" Padding="6,1" Margin="0,0,6,4">
                                                            <TextBlock Text="not present"
                                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                                       FontSize="10" FontWeight="SemiBold"/>
                                                        </Border>
                                                    </WrapPanel>
                                                    <TextBlock Text="{Binding Description}"
                                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                               FontSize="12" Margin="0,3,0,0"
                                                               TextWrapping="Wrap"/>
                                                    <TextBlock Text="{Binding CurrentText}"
                                                               Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                               FontSize="11" Margin="0,6,0,0"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Orientation="Vertical"
                                                            IsEnabled="{Binding IsPresent}">
                                                    <CheckBox Content="Monitor" IsChecked="{Binding Monitor}"/>
                                                    <CheckBox Content="Disable this task" IsChecked="{Binding DesiredDisabled}" Margin="0,4,0,4"/>
                                                    <CheckBox Content="Auto-apply silently" IsChecked="{Binding AutoApply}"/>
                                                </StackPanel>
                                            </Grid>
                                                <Expander Header="Learn more" Margin="0,8,0,0"
                                                          Visibility="{Binding LearnMoreVisibility}">
                                                    <TextBox Text="{Binding LearnMoreContent, Mode=OneWay}"
                                                             Style="{StaticResource CopyableLearnMoreText}"
                                                             Margin="8,8,8,8"/>
                                                </Expander>
                                            </StackPanel>
                                        </ui:Card>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
</UserControl>
